# Local Docker Compose defaults only. Never reuse this password outside local development.
POSTGRES_DB=hospital_coordination
POSTGRES_USER=hospital_app
POSTGRES_PASSWORD=hospital_local_only
POSTGRES_PORT=5432
